version: 2.1

jobs:
  build-and-push:
    docker:
      - image: test-build-image
    resource_class: dasmlab/Docker
    steps:
      - checkout
      - run: docker version


workflows:
  build-and-push:
    jobs:
      - build-and-push

